# 运势 AI：OpenAI 兼容接口
# - Vite 开发（默认）：中间件 `server/horoscope-api-plugin.ts` 处理 /api/horoscope
# - Vite 开发（联调 .NET）：在 .env.local 中设置其一，重启 dev 后 /api 将代理到 ASP.NET，且不再加载 Node 运势中间件
#   VITE_USE_DOTNET_API=true
#   或指定地址（可选，默认 http://localhost:5020）：
#   VITE_API_PROXY_TARGET=http://localhost:5020
# - ASP.NET：`server/AstralVeil.Api` 使用 OPENAI_* 环境变量；`dotnet run --project server/AstralVeil.Api`（默认 http://localhost:5020）
# 复制为 .env.local 后填写；不配置时将使用本地兜底文案
OPENAI_API_KEY=
# 可选，默认 https://api.openai.com/v1
# OPENAI_BASE_URL=
# 可选，默认 gpt-4o-mini
# OPENAI_MODEL=
