# 运势 AI：默认使用 DeepSeek（OpenAI 兼容 Chat Completions）
# - Vite 开发（默认）：中间件 `server/horoscope-api-plugin.ts` 处理 /api/horoscope
# - Vite 开发（联调 .NET）：在 .env.local 中设置其一，重启 dev 后 /api 将代理到 ASP.NET，且不再加载 Node 运势中间件
#   VITE_USE_DOTNET_API=true
#   同上开启时，Vite 会将 /hubs 代理到同一 Kestrel，供树洞 SignalR（Story 6.1）使用。
#   或指定地址（可选，默认 http://localhost:5020）：
#   VITE_API_PROXY_TARGET=http://localhost:5020
# - ASP.NET：`server/AstralVeil.Api` 读取下方环境变量；`dotnet run --project server/AstralVeil.Api`（默认 http://localhost:5020）
# 复制为 .env.local 后填写；不配置密钥时将无法调用模型（前端可走兜底文案）
#
# DeepSeek 控制台创建 API Key：https://platform.deepseek.com
DEEPSEEK_API_KEY=
# 若仍使用 OpenAI 或其它兼容服务，可只填 OPENAI_API_KEY，并自行设置下面两项覆盖默认 DeepSeek
# OPENAI_API_KEY=
# 可选，默认 https://api.deepseek.com/v1
# OPENAI_BASE_URL=
# 可选，默认 deepseek-chat
# OPENAI_MODEL=
